#!/usr/bin/env bash
# SPDX-License-Identifier: GPL-3.0-only
# 2026 - Anthony Burow - https://github.com/aburow
# launchers/{{PROJECT_SLUG}} — Launch the {{PROJECT_NAME}} container.
# Generated by ai-new bootstrap agent.
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_DIR="$(cd "${SCRIPT_DIR}/.." && pwd)"
PROFILE_NAME="{{PROJECT_SLUG}}"

# Locate the ai-agent-podman-sandbox bin directory.
AI_PODMAN_BIN="${AI_PODMAN_BIN:-${AI_PODMAN_JAILS_DIR:-${HOME}/ai-podman-jails}/bin}"

exec "${AI_PODMAN_BIN}/ai-launch" "${PROFILE_NAME}" "$@"
