#!/usr/bin/env bash
# launchers/build-{{PROJECT_SLUG}}.sh — Build or rebuild the {{PROJECT_NAME}} image.
# Generated by ai-new bootstrap agent.
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_DIR="$(cd "${SCRIPT_DIR}/.." && pwd)"
IMAGE_DIR="${PROJECT_DIR}/image"
IMAGE_TAG="localhost/{{PROJECT_SLUG}}:latest"

echo "Building ${IMAGE_TAG} from ${IMAGE_DIR}/Containerfile …"
podman build -f "${IMAGE_DIR}/Containerfile" -t "${IMAGE_TAG}" "${IMAGE_DIR}"
echo "Build complete: ${IMAGE_TAG}"
