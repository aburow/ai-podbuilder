# .env.example — {{PROJECT_NAME}}
# Copy this file to .env and fill in real values.
# .env is gitignored — never commit real secrets.
#
# Load in your shell before launching: set -a; source .env; set +a
# Or mount via: podman run --env-file .env ...

# ── Secrets & API keys ────────────────────────────────────────────────────────
# {{SECRET_VAR_1}}=<your-value-here>
# {{SECRET_VAR_2}}=<your-value-here>

# ── Runtime configuration ─────────────────────────────────────────────────────
# {{CONFIG_VAR_1}}=<default-value>
